// after document loaded
$(document).ready(function(){ 

	// Get page name to know which submenu to load
	// $('#pageContent_subNav').load("/submenu/products.html", function(){

		// Get class of each "a" tag
		// $("#pageContent_subNav a").each(function(index) {
			//	alert(index);
		// });
	// });
});

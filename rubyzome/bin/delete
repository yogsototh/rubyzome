#!/usr/bin/env zsh

directory=$0:h
if [[ "" = $directory ]]; then
    make_request='make_request'
else
    make_request=$directory'/make_request'
fi
# print -- "cmd: $make_request GET $*"
eval $make_request DELETE $*
